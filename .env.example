# IndexTTS API 环境变量配置示例

# 1. 模型路径配置
# 也可以使用 HuggingFace 仓库名，脚本内部会自动下载
MODEL_DIR=checkpoints/IndexTTS-2-vLLM

# 2. 网络服务配置
HOST=0.0.0.0
PORT=6006

# 3. 显存与性能优化
# 是否使用 FP16 精度 (1: 启用, 0: 禁用)
IS_FP16=0
# VLLM 显存占用率 (0.1 - 0.9)
GPU_MEMORY_UTILIZATION=0.25
# Qwen 情感模型显存占用率 (建议 0.1 左右)
QWENEMO_GPU_MEMORY_UTILIZATION=0.10

# 4. 功能开关
# 是否启用 Qwen 情感模型 (1: 启用, 0: 禁用)
ENABLE_QWEN_EMO=1
# 是否开启详细日志输出 (1: 启用, 0: 禁用)
VERBOSE=0

# 5. 其他内部变量
# 是否使用模型库 (1: ModelScope, 0: HuggingFace)
VLLM_USE_MODELSCOPE=1
